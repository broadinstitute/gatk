# Minimal set of packages to tensorize using Dataflow on OSX-64

name: ml4cvd_dataflow
channels:
  - defaults
  - anaconda
dependencies:
  - python==3.6.2
  - pip:
    - apache-beam[gcp]==2.12.0
    - google-cloud-storage==1.13.0
    - h5py==2.9.0
