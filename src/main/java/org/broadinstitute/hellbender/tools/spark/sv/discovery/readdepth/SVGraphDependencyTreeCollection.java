package org.broadinstitute.hellbender.tools.spark.sv.discovery.readdepth;

public final class SVGraphDependencyTreeCollection {
/*
    private final class SVGraphDependencyTreeNode {
        final SVGraph parent;
        final Collection<SVGraph> children;

        public SVGraphDependencyTreeNode(final SVGraph parent, final CollectioN<SVGraph>)
    }

    private final Collection<SVGraphDependencyTreeNode> roots;

    public SVGraphDependencyTreeCollection(Collection<SVGraph> graphs) {
        final SVIntervalTree<SVGraph> graphIntervalTree =  new SVIntervalTree();
        for (final SVGraph graph : graphs) {
            for (final SVInterval interval : graph.getContigIntervals()) {
                graphIntervalTree.put(interval, graph);
            }
        }
    }
    */
}
