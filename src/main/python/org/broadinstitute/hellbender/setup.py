from setuptools import setup

setup(name='gatkpython',
      version='0.1',
      description='Testing install from archive',
      author='GATK Team',
      packages=['gatkpython'],
      zip_safe=False)

