codecov:
  branch: master
  bot: null

coverage:
  precision: 3
  round: nearest
  range: "0...100"

comment:
  layout: "header, diff, changes, sunburst, uncovered, tree"
  branches: null
  behavior: default
