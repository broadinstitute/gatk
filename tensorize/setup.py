from setuptools import setup, find_packages

setup(
    name='ml_tensorize',
    version='0.1.1',
    description='ml4cvd tensorization package.',
    install_requires=[],
    packages=find_packages(),
)
