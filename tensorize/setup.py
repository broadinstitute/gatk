from setuptools import setup, find_packages

setup(
    name='ml_tensorize',
    version='0.0.1',
    description='ml tensorization package.',
    install_requires=[],
    packages=find_packages(),
)
